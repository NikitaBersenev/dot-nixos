# Local OpenAI tunnel / Codex secrets
.env
.env.*
!.env.example
*.local

# Local Codex state; project config in .codex/config.toml may be committed separately if needed.
.codex/auth.json
.codex/history.jsonl
.codex/sessions/
